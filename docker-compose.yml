services:
  app:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      DATA_DIR: /data
      HTTP_ADDR: :8080
    volumes:
      - ./data:/data
    restart: unless-stopped
